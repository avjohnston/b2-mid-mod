<h1> <%= @mechanic.name %> </h1>
<p>Years Experience: <%= @mechanic.years_experience %></p>

<h3>Current Rides: </h3>
<% @rides.each do |ride| %>
  <p><%= ride.name %></p>
<% end %>

<h3>Add A Ride to Workload</h3>
<%= form_with url: "/mechanics/#{@mechanic.id}", method: :patch, local: true, params: {ride_id: :ride_id} do |f| %>
  <%= f.label          "ride ID" %>
  <%= f.number_field     :ride_id %><br>
  <%= f.submit 'Submit' %>
<% end %>
